Projet Producteur-Consommateur (en Programmation concurrente)

Noms des binomes:
	- Ali FAWAZ
	- Pamella HANI

Ce projet contient plusieurs packages Java, conformément aux objectifs demandés. Le package associé à l'objectif 1 est `prodcons.v1`. Par exemple, pour tester l'objectif 1, la fonction main de chaque objectif se trouve dans le fichier `TestProdCons.java`.

Un fichier commun pour tous les objectifs est présent dans le répertoire `src`, dont l'adresse est `src/options.xml`.